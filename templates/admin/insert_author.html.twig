{% extends 'admin/base.html.twig' %}

{% block main %}
    {#    J'utilise la fonction twig form et je lui passe la variable qui contient mon formulaire depuis le controleur ça permet de geneger tout les inputs + label de mon formulaire#}
    <div class="form-group">
        {#        {{ form(form) }}#}
        {{ form_start(form) }}

        {{ form_label(form.firstName, 'Prenom',{'label_attr':{'class': 'form-label' }}) }}
        {{ form_widget(form.firstName, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.firstName) }}

        {{ form_label(form.lastName, 'Nom de famille',{'label_attr':{'class': 'form-label'}}) }}
        {{ form_widget(form.lastName, {'attr':{'class': 'form-control'}}) }}
        {{ form_errors(form.lastName) }}

        {{ form_label(form.birthDate, 'Date de naissance',{'label_attr':{'class':'form-label'}}) }}
        {{ form_widget(form.birthDate, {'attr':{'class': 'form-control'}}) }}
        {{ form_errors(form.birthDate) }}

        {{ form_widget(form.submit, {'attr':{'class': 'btn btn-primary'}}) }}
        {{ form_end(form) }}
    </div>

{% endblock %}