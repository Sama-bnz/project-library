{% extends 'base.html.twig' %}

{% block title %}
    <title>La liste des oeuvres</title>
{#    <a href="{{ path('admin_insert_book')}}">Ajouter un livre</a>#}
{% endblock %}

{% block main %}
    {% for book in books %}
        <article>
            <h1>{{ book.title }}</h1>
            <p>{{ book.nbPages }}</p>
            <p>{{ book.author }}</p>
            <a href="{{ path('admin_show_author',{'id':author.id}) }}">En savoir plus sur le livre</a>
            <br>
{#            <a href="{{ path ('admin_update_author',{'id':author.id })}}">Voulez-vous modifié les données de ce livre?? </a>#}


        </article>

    {% endfor %}
{% endblock %}

