{% extends 'base.html.twig' %}

{% block title %}
    <title>{{ author.firstName }}</title>
{% endblock %}

{% block main %}

    <article class="auteurs_seul">
        <h1 class="titre">{{ author.firstName }} {{ author.lastName }}</h1>
        <p>Né le {{ author.birthDate|date('Y-m-d') }}</p>
        <a href="{{ path('front_authors',{'id':author.id}) }}">La liste des auteurs</a>
    </article>
{% endblock %}